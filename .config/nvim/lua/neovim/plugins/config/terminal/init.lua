
-- ... init.lua

-- hold configurations for `terminal` namespace that need to be
-- loaded after the configurations of each plugin under `terminal`
-- namespace is loaded

local Terminal = {}

function Terminal.configure()
  -- configure terminal plugins here.
end

return Terminal

