
-- ... init.lua

-- hold configurations for `environment` namespace that need to be
-- loaded after the configurations of each plugin under `environment`
-- namespace is loaded

local Environment = {}

function Environment.configure()
  -- configure environment plugins here.
end

return Environment

